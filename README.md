# Auto.LLM
Allow an LLM to completely access a computer, by itself.

Note:
We recommend you run this in Docker, on a virtual machine, on a closed/private network. This was originally designed to be an experiment to see what would LLMs do if they owned an entire computer.
